
void drop_dow()
{/* cleardevice();
  dis_img("images\\dragon",180,150);
  settextstyle(2,0,7);
  for(int y=0,x=20;y<=150;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("P");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("P");
  } //for
  setcolor(WHITE);
  outtextxy(x,y,"P");
  for(y=0,x=30;y<=150;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("R");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("R");
  } //for
  setcolor(WHITE);
  outtextxy(x,y,"R");

  for( y=0,x=40;y<=150;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("O");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("O");
  } //for
  setcolor(WHITE);
  outtextxy(x,y,"O");

  for( y=0,x=50;y<=150;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("J");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("J");
  } //for
  setcolor(WHITE);outtextxy(x,y,"J");

  for( y=0,x=60;y<=150;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("E");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("E");
  } //for
  setcolor(WHITE);outtextxy(x,y,"E");

  for( y=0,x=70;y<=150;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("C");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("C");
  } //for
  setcolor(WHITE);outtextxy(x,y,"C");

  for( y=0,x=80;y<=150;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("T");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("T");
  } //for
  setcolor(WHITE);outtextxy(x,y,"T");

  for( y=0,x=100;y<=150;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("B");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("B");
  } //for
  setcolor(WHITE);outtextxy(x,y,"B");

  for( y=0,x=110;y<=150;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("Y");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("Y");
  } //for
  setcolor(WHITE);outtextxy(x,y,"Y");

  for( y=0,x=120;y<=150;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext(":");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext(":");
  } //for
  setcolor(WHITE);outtextxy(x,y,":");

  settextstyle(1,0,2);
  for( y=0,x=130;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("C");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("C");
  } //for
  setcolor(WHITE);outtextxy(x,y,"C");

  for( y=0,x=143;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("H");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("H");
  } //for
  setcolor(WHITE);outtextxy(x,y,"H");

  for( y=0,x=156;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("A");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("A");
  } //for
  setcolor(WHITE);outtextxy(x,y,"A");

  for( y=0,x=169;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("N");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("N");
  } //for
  setcolor(WHITE);outtextxy(x,y,"N");

  for( y=0,x=182;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("D");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("D");
  } //for
  setcolor(WHITE);outtextxy(x,y,"D");

  for( y=0,x=195;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("A");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("A");
  } //for
  setcolor(WHITE);outtextxy(x,y,"A");

  for( y=0,x=208;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("N");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("N");
  } //for
  setcolor(WHITE);outtextxy(x,y,"N");

  for( y=0,x=231;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("&");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("&");
  } //for
  setcolor(WHITE);outtextxy(x,y,"&");

  for( y=0,x=250;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("H");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("H");
  } //for
  setcolor(WHITE);outtextxy(x,y,"H");

  for( y=0,x=263;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("A");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("A");
  } //for
  setcolor(WHITE);outtextxy(x,y,"A");

  for( y=0,x=276;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("R");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("R");
  } //for
  setcolor(WHITE);outtextxy(x,y,"R");

  for( y=0,x=289;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("S");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("S");
  } //for
  setcolor(WHITE);outtextxy(x,y,"S");

  for( y=0,x=302;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("H");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("H");
  } //for

  setcolor(WHITE);outtextxy(x,y,"H");
  for( y=0,x=315;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("I");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("I");
  } //for
  setcolor(WHITE);outtextxy(x,y,"I");

  for( y=0,x=328;y<=180;y++)
  {setcolor(WHITE);
  moveto(x,y);
  outtext("T");
  delay(1);
  setcolor(BLACK);
  moveto(x,y);
  outtext("T");
  } //for
  setcolor(WHITE);outtextxy(x,y,"T");
 delay(2000); */
}//main